import React from "react";
import Header from "./includes/header";

const Admin: React.FC = () => {
    return (
        <div>
            <Header />
            <div>Admin</div>
        </div>
    );
};

export default Admin;
